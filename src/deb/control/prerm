#!/bin/bash
set -e

# Stop and disable service before removal
if [ -d /run/systemd/system ] && [ "$1" = "remove" ]; then
    systemctl stop alpidi-printer-agent.service || true
    systemctl disable alpidi-printer-agent.service || true
fi

#DEBHELPER#

exit 0